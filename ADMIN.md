# ממשק הניהול

כתובת: **`https://www.danielkitchens.com/admin`**

הכפתור לא מופיע בשום מקום באתר, והכתובת חסומה למנועי חיפוש. מי שלא יודע את הכתובת
ואת הסיסמה לא מגיע לשם.

## מה אפשר לעשות שם

- לערוך כל גלריה קיימת: שם, כותרת משנה, תיאורים, מפרט, קרדיטים
- להוסיף תמונות, למחוק, לשנות סדר, לבחור תמונת שער ולכתוב תיאור לכל תמונה
- לפתוח גלריה חדשה או למחוק גלריה
- לשנות את סדר הגלריות בדף הבית
- לבחור אילו תמונות רצות בראש דף הבית

לחיצה על **שמירה ופרסום** יוצרת commit ב-GitHub, ו-Vercel בונה את האתר מחדש. השינוי
באוויר תוך כדקה.

## הגדרה חד־פעמית

בלי שלושת המשתנים האלה הפאנל עולה אך אי אפשר להתחבר אליו.

### 1. סיסמה ומפתח חתימה

מריצים מקומית, עם סיסמה חזקה משלכם:

```bash
npm run admin:secrets -- "הסיסמה-שבחרתם"
```

הפקודה מדפיסה שתי שורות — `ADMIN_PASSWORD_HASH` ו-`ADMIN_SESSION_SECRET`. הסיסמה עצמה
לא נשמרת בשום מקום; מה שנשמר הוא גיבוב scrypt שלה.

### 2. מפתח גישה ל-GitHub

ב-GitHub: **Settings → Developer settings → Personal access tokens → Fine-grained tokens**.

- Repository access: **Only select repositories** → `daniel-kitchens-landing-page`
- Permissions → Repository permissions → **Contents: Read and write**
- שאר ההרשאות: ללא

### 3. הזנה ב-Vercel

**Project → Settings → Environment Variables**, לסביבת Production:

| שם | ערך |
|---|---|
| `ADMIN_PASSWORD_HASH` | מהפקודה בשלב 1 |
| `ADMIN_SESSION_SECRET` | מהפקודה בשלב 1 |
| `GITHUB_TOKEN` | המפתח משלב 2 |
| `GITHUB_REPO` | `ohadk1/daniel-kitchens-landing-page` |
| `GITHUB_BRANCH` | `main` (אפשר להשמיט) |

אחרי ההזנה צריך deploy אחד כדי שהמשתנים ייכנסו לתוקף.

**אף אחד מהערכים האלה לא נכנס לקוד ולא ל-GitHub.**

## איך התמונות מטופלות

התמונה מוקטנת וממירה ל-WebP **בדפדפן**, לפני שהיא נשלחת. שתי סיבות: תמונה מהטלפון היא
כמה מגה־בייט וחורגת ממגבלת הבקשה, והאתר מגיש רק WebP.

- צלע ארוכה מרבית: 2400 פיקסלים
- סיבוב EXIF מיושם, כך שתמונות מהטלפון לא יוצאות שוכבות
- שם הקובץ הוא גיבוב של התוכן — `smoked-424575ed8e.webp`

הגיבוב בשם חשוב: התיקייה `/images` מוגשת עם `immutable`, כלומר הדפדפן שומר קובץ שנה
שלמה בלי לבדוק שוב. שם שנגזר מהתוכן מבטיח שהחלפת תמונה תמיד תיראה מיד.

## אם משהו משתבש

כל שמירה היא commit. אפשר לראות את ההיסטוריה ב-GitHub ולחזור לכל נקודה:

```bash
git revert <commit>
```

תמונה נמחקת מהמאגר רק אם אף גלריה ואף שקופית לא מפנות אליה יותר.
